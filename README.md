
  # Quiz Builder with Scoring

  This is a code bundle for Quiz Builder with Scoring.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
